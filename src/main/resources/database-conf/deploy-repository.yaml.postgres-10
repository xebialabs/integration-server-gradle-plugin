xl.repository:
  artifacts:
    root: build/artifacts
  database:
    db-driver-classname: "org.postgresql.Driver"
    db-url: "jdbc:postgresql://localhost:{{DB_PORT}}/xldrepo"
    db-username: "postgres"
    db-password: "demo"
    leak-detection-threshold: 2 minutes
    max-pool-size: 10
